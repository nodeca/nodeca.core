// Toolbar with buttons

@import "nodeca.core/client/common/_variables"

// Container to preserve space, when controlbar become float
.controlbar
  height: 56px

// Hack bootstrap to avoid double padding
// on nested .container block for controlbar
.controlbar .container
  padding 0

.controlbar__body
  //padding 10px 15px 10px
  margin-left 0
  padding-bottom 10px
  background-color white
  //border-radius 4px
  border-bottom 1px solid grayLighter
  box-shadow 0 0 2px #f8f8f8
  color grayLight
  //max-width pageMaxWidth

// floating state modificator
.controlbar.affix
  // Don't change global container position,
  // it should stay intact to reserver space
  position static

  .controlbar__floater
    position fixed
    width 100%
    top 0
    z-index 1000
    background-color white

  .controlbar__body
    border 0
    border-radius 0
    padding 10px 15px 10px

.nav-vert-on .controlbar.affix
  .controlbar__floater
    max-width pageMaxWidth
    margin-left -(navPanelWidth + (gridGutterWidth / 2))
    padding-left navPanelWidth

    border-bottom 1px solid grayLighter
    box-shadow 0 4px 2px -2px #f8f8f8

.nav-horiz-on .controlbar.affix
  .controlbar__floater
    left 0
    right 0
    border-bottom 1px solid grayLighter
    box-shadow 0 4px 2px -2px #f8f8f8
  .controlbar__floater-inner
    margin-left auto
    margin-right auto
    max-width pageMaxWidth
