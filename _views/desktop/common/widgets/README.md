Widgets are parts of page, that requires additional data in response `widgets` subtree.
Those can be skipped, when we change main content. For example, when we add `next page`
via rpc.

Don't confuse with partials.