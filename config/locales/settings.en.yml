i18n:
  en-US:
    admin:
      core:
        group_names:
          core: Core
          general: General
          email: Email
          content: Content

        category_names:
          general_common: Common
          email_common: Common
          content_common: Common

        setting_names:
          general_project_name: Project name
          general_project_name_help: Used in subject line of automatic email postings.

          general_session_expire_hours: Session expire time (hours)
          general_session_expire_hours_help: Session store period since last visit.

          general_cookies_domain: Cookies domain
          general_cookies_domain_help: Leave empty to auto-detect. (when site use single domain)

          email_transport: Transport

          email_header_from: Outgoing email
          email_header_from_help: Email address for auto-generated letters
            (email validation, notifications, etc)

          content_read_marks_expire: Max time to store read marks, older content considered read (days)

        setting_values:
          email_transport:
            sendmail: sendmail
            dummy: dummy (only log, for debug)
