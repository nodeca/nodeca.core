i18n:
  en-US:
    admin:
      settings:
        debug_group:
          title: Debug mode

        debug_enabled:
          title: Activate Debug Mode
          description: Set this setting to 'yes' to have debug information shown to all users with access to your ACP.

        debug_ip:
          title: Debug Mode for a specific IP-Address
          description: Enter your IP address here, so that debug mode will only run for you. 

        debug_users:
          title: User
          description: Choose users for which your forums should run in Debug Mode.
