menus:
  common:
    admin:
      dashboard:
        to: admin.core.dashboard.show
        priority: 500
        check_permissions: true

  admin:
    navbar:
      core:
        to: admin.core.dashboard.show
        submenu:
          dashboard:
            to: admin.core.dashboard.show

          global_settings:
            to: admin.core.global_settings.index
