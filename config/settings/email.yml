setting_groups:
  email:
    parent: core


setting_schemas:
  global:
    email_transport:
      priority: 10
      group_key: email
      category_key: email_common
      type: dropdown
      values:
      - sendmail: SENDMAIL
      - dummy:    STUB     # only log, for debug
      default: SENDMAIL

    email_header_from:
      priority: 20
      group_key: email
      category_key: email_common
      type: string
      default: noreply@example.com
